from django.apps import AppConfig


class CoreAppConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'core_app'

# So, while i am free soul, that can think freely, i want to say:
# We shall be able to make good, not listen to those, who cant rule.